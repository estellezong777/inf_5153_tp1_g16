// Trois types de message.
// 'normalText' indique que c'est le text normal que l'agent envoie.
//'ACK' indique que Message d’accusé de réception
//'noReceiver' indique que le receveur n’existe pas (ou plus).
public enum msgType {
    normalText,
     ACK,
     noReceiver
}
